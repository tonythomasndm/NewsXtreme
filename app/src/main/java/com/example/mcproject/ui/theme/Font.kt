package com.example.mcproject.ui.theme

import androidx.compose.ui.text.font.Font
import androidx.compose.ui.text.font.FontFamily
import com.example.mcproject.R

val SemiBold = FontFamily(
    Font(resId = R.font.montserrat_semibold)
)

val Black = FontFamily(
    Font(resId = R.font.montserrat_black)
)
val BlackItalic = FontFamily(
    Font(resId = R.font.montserrat_black_italic)
)
val Bold = FontFamily(
    Font(resId = R.font.montserrat_bold)
)
val BoldItalic = FontFamily(
    Font(resId = R.font.montserrat_bold_italic)
)
val ExtraBold = FontFamily(
    Font(resId = R.font.montserrat_extrabold)
)
val ExtraBoldItalic = FontFamily(
    Font(resId = R.font.montserrat_extrabold_italic)
)
val ExtraLight = FontFamily(
    Font(resId = R.font.montserrat_extralight)
)
val ExtraLightItalic = FontFamily(
    Font(resId = R.font.montserrat_extralight_italic)
)
val Italic = FontFamily(
    Font(resId = R.font.montserrat_italic)
)
val Light = FontFamily(
    Font(resId = R.font.montserrat_light)
)
val LightItalic = FontFamily(
    Font(resId = R.font.montserrat_light_italic)
)
val Medium = FontFamily(
    Font(resId = R.font.montserrat_medium)
)
val MediumItalic = FontFamily(
    Font(resId = R.font.montserrat_medium_italic)
)
val Regular = FontFamily(
    Font(resId = R.font.montserrat_regular)
)
val SemiBoldItalic = FontFamily(
    Font(resId = R.font.montserrat_semibold_italic)
)
val Thin = FontFamily(
    Font(resId = R.font.montserrat_thin)
)
val ThinItalic = FontFamily(
    Font(resId = R.font.montserrat_thin_italic)
)